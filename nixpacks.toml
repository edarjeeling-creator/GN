providers = ["node"]

[variables]
NODE_OPTIONS = "--max-old-space-size=2048"

[phases.install]
cmds = ['npm ci --no-audit --prefer-offline']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run preview'
